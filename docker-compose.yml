# docker-compose.yml
version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./exercises:/app/exercises
      - ./utils:/app/utils
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped